import { PayButton } from "@/widgets/PayButton/PayButton";

export default function Home() {
  return (
    <main>
      <section id="payment">
        <PayButton />
      </section>
    </main>
  );
}
